---
description: MCP | Generate CTX MCP config for OpenCode
---

Generate the CTX MCP config snippet for OpenCode.

!`'/usr/local/bin/ctx' --repo-root '/Users/supporterino/WebstormProjects/Shelly-Manager' mcp config opencode`

Then show the output and explain how to use it.
