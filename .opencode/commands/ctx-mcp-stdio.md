---
description: MCP | Show the CTX MCP stdio launch command
---

Show the CTX MCP stdio launch command for the current repository.

Use the current repository root and explain how a host CLI can launch `'/usr/local/bin/ctx' --repo-root '/Users/supporterino/WebstormProjects/Shelly-Manager' mcp stdio` locally.
