---
description: Context | Build a compact CTX task context pack
---

Build a compact CTX context pack for this task:

$ARGUMENTS

!`'/usr/local/bin/ctx' --repo-root '/Users/supporterino/WebstormProjects/Shelly-Manager' pack "$ARGUMENTS" --json`

Print `compact_context` first.
Then print one compact stats line with `packed_tokens`, `reduction_pct`, and `pack_path`.
Keep any follow-up explanation to at most one short sentence.
